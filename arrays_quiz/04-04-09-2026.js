// Create an array called permissions. each object of the permission array
// contains keys such as : permission id, resource name, action name,
// description, is_active(boolean),

const permissions = [
 {
  'permission id' : '999JIO',
  'resource name' : 'keys',
  'action name' : 'padlock',
  'description' : 'under review',
  'is_active' : 'true',
 }
]

console.log(permissions);

