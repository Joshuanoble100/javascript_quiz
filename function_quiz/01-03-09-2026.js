// Create a function named (createServer). The function should return an object called (Transactions).
 //The transaction object contain various keys such as 
  // Transaction ID, ammount, payment date, user id, product id, order id

function createServer() {
 const transaction = {
  'transaction id' : '21ABUI3',
  'ammount' : 20000,
  'payment date' : '25 september 2026',
  'user id' : '45RRE',
  'product id' : 'ERT567',
  'order id' : 'RRR000'
 }
 return transaction
 console.log(transaction);
}
createServer();











